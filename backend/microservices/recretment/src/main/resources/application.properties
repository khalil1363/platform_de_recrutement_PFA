spring.application.name=RECRUITMENT

server.port=8090

spring.datasource.url=jdbc:mysql://localhost:3306/REC?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=000000
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

jwt.secret=${JWT_SECRET:K7mN9pQ2xR5vL8wY1zA4bC6dE0fG3hJ5kM8nP1qS4tU7vW0xY3zA6bC9dE2fG5hJ8kM=}
jwt.expiration=86400000

internal.api-key=${INTERNAL_API_KEY:daam-internal-secret-key}

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
file.upload-dir=uploads/cv

eureka.client.register-with-eureka=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

logging.level.com.daam.recruitment=DEBUG

# Load local secrets (email password, Meet room URL) — file is gitignored
spring.config.import=optional:classpath:application-local.properties

# Email — password goes in application-local.properties only
app.mail.enabled=${MAIL_ENABLED:false}
app.mail.from=${MAIL_FROM:khalil.fray@issatm.ucar.tn}
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:khalil.fray@issatm.ucar.tn}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Google Calendar API — creates a real Meet link for each interview date


# CV analysis (local skill matching by default; optional OpenAI for richer analysis)
app.cv-analysis.enabled=true
app.cv-analysis.openai-api-key=${OPENAI_API_KEY:}
app.cv-analysis.openai-model=${OPENAI_MODEL:gpt-4o-mini}
